# 2026-08-31 项目周报

## 本周状态

运行与交付文档已补齐，覆盖本地环境建立、Neo4j 配置、CSV 校验与导入、FastAPI、React 前端和四类功能演示。文档以当前仓库脚本和接口为准，未把未复核数据或占位评测写成正式结果。

## 当前可交付内容

- `docs/setup.md`：从克隆仓库到启动单服务版本的操作步骤。
- `docs/demo-script.md`：部件关联、缺陷处理、限度标准、工序步骤四类演示口径，以及证据字段检查项。
- `docs/troubleshooting.md`：环境、Neo4j、CSV、API 和前端常见故障处理。
- 正式导入表 `data/import/entities.csv`、`data/import/relations.csv` 当前包含 65 个实体和 55 条关系，全部为 `reviewed`。
- 成员 D 首批数据经组长复核后纳入正式表，保留 60 个实体和 50 条关系；成员 C 的 48 个实体和 47 条关系已完成结构与证据修复，但与正式样例存在同名概念，暂保留为 `draft`，未导入正式表。
- `data/evaluation/questions.csv` 当前包含 16 道 `reviewed` 题目，部件关联、缺陷处理、限度标准、工序步骤各 4 道。
- 当前运行环境的 `GET /health` 已返回 `status=ok`、`database=connected`；四类示例自然语言问题均返回 `found=true` 和证据。

## 可验证限制

- Neo4j 需要本机运行并通过 `bolt://localhost:7687` 连接；数据库停止时不能完成真实导入和问答验收。本周记录的运行环境已通过 Bolt 端口和 API 健康检查。
- 当前正式导入表已包含 6 个 `Procedure` 节点和 5 条 `NEXT_STEP` 关系，可演示“清洗轴承→轴承外观检查”等工序链。
- 评测脚本会读取 16 道 `reviewed` 题目；只有在 Neo4j 和 API 健康检查通过后，运行结果才代表真实评测。

## 复核命令

```powershell
.\.venv\Scripts\python.exe -m scripts.validation.validate_csv
Test-NetConnection -ComputerName 127.0.0.1 -Port 7687
Invoke-RestMethod http://127.0.0.1:8000/health
```

以上命令分别用于确认 CSV 结构、Neo4j Bolt 端口和应用数据库状态。真实数据验收应以接口返回的 `found`、证据双页码和规程原文为准。
